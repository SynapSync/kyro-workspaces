---
title: "kyro-sprint-forge-reader — Adaptive Roadmap"
date: "2026-03-05"
updated: "2026-03-06"
project: "kyro-sprint-forge-reader"
type: "plan"
status: "active"
version: "1.0"
agents:
  - "claude-opus-4-6"
tags:
  - "kyro-sprint-forge-reader"
  - "plan"
  - "roadmap"
  - "sprint-forge"
changelog:
  - version: "2.0"
    date: "2026-03-07"
    changes: ["Phase 2 roadmap — growth-ceo initiatives (sprints 7-10)"]
  - version: "1.0"
    date: "2026-03-05"
    changes: ["Roadmap created"]
related:
  - "[[README]]"
  - "[[RE-ENTRY-PROMPTS]]"
---

# kyro-sprint-forge-reader — Adaptive Roadmap

> Generated by `sprint-forge` on 2026-03-05

---

## Project Paths

| Path              | Value                                                                                        |
| ----------------- | -------------------------------------------------------------------------------------------- |
| Codebase          | `/Users/rperaza/joicodev/ideas/kyro`                                                         |
| Working Directory | `/Users/rperaza/joicodev/ideas/kyro/.agents/sprint-forge/kyro-sprint-forge-reader`           |
| Findings          | `/Users/rperaza/joicodev/ideas/kyro/.agents/sprint-forge/kyro-sprint-forge-reader/findings/` |
| Sprints           | `/Users/rperaza/joicodev/ideas/kyro/.agents/sprint-forge/kyro-sprint-forge-reader/sprints/`  |

---

## Execution Rules

These rules govern the entire sprint lifecycle. They are non-negotiable.

1. **Sprint-by-Sprint** — Sprints are generated ONE AT A TIME. Never pre-generate all sprints. Each sprint is informed by the results of the previous one.
2. **Retro Feeds Forward** — The retrospective and recommendations of Sprint N-1 are formal inputs for Sprint N. They cannot be ignored.
3. **Recommendations are Dispositioned** — Every recommendation from the previous sprint must either become a task or have its deferral justified in the Disposition table.
4. **Debt is Inherited** — The Accumulated Technical Debt table passes from sprint to sprint in full. Items are never deleted, only resolved.
5. **Phases are Suggested** — The roadmap defines suggested phases per sprint. During execution, emergent phases MUST be added when new work is discovered.
6. **Emergent Findings Generate Phases** — If execution reveals issues not covered by the plan, new phases are added to the current sprint.
7. **Re-entry Prompts are Updated** — After INIT and after each executed sprint, re-entry prompts are refreshed to reflect current state.
8. **Roadmap Adapts** — If execution reveals that planned sprints need modification, this roadmap is updated. The plan serves execution, not the reverse.
9. **Definition of Done** — Each sprint has explicit completion criteria. A sprint is not done until all DoD items are checked.

---

## Task States

| Symbol | State       | Meaning                                     |
| ------ | ----------- | ------------------------------------------- |
| `[ ]`  | Pending     | Not started                                 |
| `[~]`  | In Progress | Currently being worked on                   |
| `[x]`  | Done        | Completed and verified                      |
| `[!]`  | Blocked     | Cannot proceed — blocker documented         |
| `[-]`  | Skipped     | Intentionally skipped with justification    |
| `[>]`  | Carry-over  | Not completed — carried over to next sprint |

---

## Sprint Template

Each sprint in this roadmap follows this structure:

| Field            | Description                                                            |
| ---------------- | ---------------------------------------------------------------------- |
| Sprint #         | Sequential number                                                      |
| Finding Source   | Which finding file(s) this sprint addresses                            |
| Version Target   | Target version after this sprint (if applicable)                       |
| Type             | audit / refactor / feature / bugfix / debt                             |
| Focus            | One-line description of the sprint's primary goal                      |
| Suggested Phases | Initial phases suggested by this roadmap (may expand during execution) |
| Dependencies     | Which sprints must complete before this one                            |

---

## Dependency Map

```
Phase 1 (Reader — completed):
Sprint 1 → Sprint 2 → Sprint 3 → Sprint 4 → Sprint 5 → Sprint 6

Phase 2 (Cockpit — growth-ceo initiatives):
Sprint 7 (search) ──────────────────────────────┐
Sprint 8 (actions + kanban persistence) ─────────┼→ Sprint 10 (AI instruction layer)
Sprint 9 (sprint-forge trigger) ─────────────────┘
```

Phase 2 sprints 7-9 are independent of each other (all build on the completed Phase 1 foundation). Sprint 10 depends on all three.

---

## Sprint Summary

| Sprint | Finding Source                                                          | Version | Type     | Focus                                                  | Dependencies | Status    |
| ------ | ----------------------------------------------------------------------- | ------- | -------- | ------------------------------------------------------ | ------------ | --------- |
| 1      | `01-sprint-file-format`, `02-task-format`, `04-missing-domain-concepts` | 0.2.0   | refactor | Domain types and sprint-forge parsers                  | —            | completed |
| 2      | `03-project-model`, `07-workspace-config`                               | 0.3.0   | refactor | Project registry, API routes, workspace model          | Sprint 1     | completed |
| 3      | `05-sprint-sections`, `06-roadmap-readme`                               | 0.4.0   | refactor | Service layer, store adaptation, section extraction    | Sprint 2     | completed |
| 4      | `08-ui-adaptation`                                                      | 0.5.0   | feature  | UI adaptation: sprint detail, kanban, project creation | Sprint 3     | completed |
| 5      | (cross-cutting)                                                         | 1.0.0   | feature  | New views (findings, roadmap, debt), E2E, polish       | Sprint 4     | completed |
| 6      | (user-reported bug)                                                     | 1.1.0   | bugfix   | Markdown rendering pipeline fix                        | Sprint 5     | completed |
| 7      | `09-live-search`                                                        | 2.0.0   | feature  | Universal search + command palette enhancement         | Sprint 6     | completed |
| 8      | `10-command-bar-actions`                                                 | 2.1.0   | feature  | Kanban persistence + structured write actions          | Sprint 6     | completed |
| 9      | `11-sprint-forge-trigger`                                               | 2.2.0   | feature  | Sprint generation wizard + prompt composer             | Sprint 6     | pending   |
| 10     | `09`, `10`, `11` (convergence)                                          | 3.0.0   | feature  | AI instruction layer + autonomous agent loop           | Sprint 7-9   | pending   |

---

## Detailed Sprint Definitions

### Sprint 1 — Domain Types & Sprint-Forge Parsers

- **Source**: `findings/01-sprint-file-format-incompatibility.md`, `findings/02-task-format-and-phase-structure.md`, `findings/04-missing-domain-concepts.md`
- **Version Target**: 0.2.0
- **Type**: refactor
- **Focus**: Create the complete type system and parsing layer for sprint-forge output
- **Dependencies**: None
- **Suggested Phases**:
  1. New Zod Types — Add all sprint-forge domain types: `SprintType`, `Finding`, `DebtItem`, `Phase`, `DispositionEntry`, `RoadmapSprintEntry`. Extend `Task` with `taskRef`, `files`, `evidence`, `verification`. Extend `Sprint` with `source`, `previousSprint`, `sprintType`, `phases`, `disposition`, `debtItems`, `definitionOfDone`.
  2. Markdown Utilities — Create a generic markdown section extractor (`extractSections`) and markdown table parser (`parseMarkdownTable`). These are reused by all subsequent parsers.
  3. Sprint-Forge Sprint Parser — Implement `parseSprintForgeFile()`: extract metadata from heading + blockquotes, parse phases/tasks with sub-items, extract all sections. Add format auto-detection (frontmatter vs sprint-forge).
  4. Finding & Roadmap Parsers — Implement `parseFindingFile()` and `parseRoadmap()`. Extract structured data from heading-based markdown.
  5. Parser Tests — Comprehensive unit tests for all new parsers using real sprint-forge sample files.

### Sprint 2 — Project Registry & API Route Refactor

- **Source**: `findings/03-project-model-external-directories.md`, `findings/07-workspace-config-and-project-registry.md`
- **Version Target**: 0.3.0
- **Type**: refactor
- **Focus**: Replace internal project directories with external directory registry and refactor all API routes
- **Dependencies**: Sprint 1
- **Suggested Phases**:
  1. Project Registry — Create `projects.json` schema, parser, serializer. API endpoints: list projects (from registry), add project (validate + register path), remove project.
  2. Path Resolution Refactor — Update `resolveAndGuard()` to support per-project root paths. Create `resolveProjectPath()` helper that reads registry.
  3. API Routes — Refactor `GET /api/projects` (read registry), `POST /api/projects` (register external dir), sprint routes (read from external path), new `GET /api/projects/[id]/findings` endpoint.
  4. Workspace Init — Update init flow: create `.kyro/projects.json` (empty). Remove `projects/` directory creation. Update workspace config.
  5. Integration Tests — Test full API flow: register project → list sprints → read sprint → read findings.

### Sprint 3 — Service Layer & Store Adaptation

- **Source**: `findings/05-sprint-sections-expansion.md`, `findings/06-roadmap-and-readme-parsing.md`
- **Version Target**: 0.4.0
- **Type**: refactor
- **Focus**: Update service contracts, file services, and Zustand store for new data model
- **Dependencies**: Sprint 2
- **Suggested Phases**:
  1. Service Interfaces — Expand `ProjectsService` with: `getFindings()`, `getFinding()`, `getRoadmap()`. Update return types to use new schemas. Add `FindingsService` if warranted.
  2. File Service Implementation — Update `projects.file.ts` to call new API routes. Implement findings service. Handle sprint-forge README parsing (blockquote metadata).
  3. Store Adaptation — Add state for findings, roadmap summary, active finding. Update sprint state to include phases, disposition, debt items. Preserve optimistic update pattern.
  4. Mock Service — Create mock data that mimics sprint-forge output structure. Update `mock-data.ts` with sample phases, findings, debt items.
  5. Section Extraction — Wire up `extractSections()` in the sprint parsing pipeline. Map sprint-forge headings to `SprintMarkdownSections` keys. Update `SPRINT_SECTIONS` config.

### Sprint 4 — UI Adaptation

- **Source**: `findings/08-ui-adaptation-kanban-and-detail-pages.md`
- **Version Target**: 0.5.0
- **Type**: feature
- **Focus**: Adapt all UI components to display sprint-forge data correctly
- **Dependencies**: Sprint 3
- **Suggested Phases**:
  1. Sprint Detail Expansion — Add new tabs (disposition, emergent phases, definition of done). Create `DebtTable`, `DispositionTable`, `DoDChecklist` components. Update tab rendering in `sprint-detail-page.tsx`.
  2. Task Card & Kanban — Update `TaskCard` to show task ref ID, files badge. Add phase grouping option to sprint board. Handle emergent task visual distinction.
  3. Project Creation Flow — Replace "create project" with "add project" dialog. Directory path input with validation. Auto-detect project name from sprint-forge README.
  4. Navigation & Sidebar — Add findings and roadmap nav items to `NAV_ITEMS`. Wire routing to new pages. Sprint type badges on sprint list.
  5. Config & Theme — Update `SPRINT_SECTIONS`, `NAV_ITEMS`, `COLUMNS` configs. Add sprint type color mapping. Add finding severity color mapping.

### Sprint 5 — New Views, E2E & Polish

- **Source**: Cross-cutting (all findings, accumulated debt from previous sprints)
- **Version Target**: 1.0.0
- **Type**: feature
- **Focus**: Build new views, comprehensive testing, and documentation
- **Dependencies**: Sprint 4
- **Suggested Phases**:
  1. Findings Browser — List view of all findings with severity badges, linked sprints, search/filter. Finding detail view (rendered markdown).
  2. Roadmap Viewer — Visual representation of sprint plan from ROADMAP.md. Sprint cards with type, status, dependencies. Consider dependency graph visualization.
  3. Debt Dashboard — Cross-sprint debt tracking view. Open items, resolution trend, origin breakdown. Reuse `DebtTable` component with aggregated data.
  4. E2E Testing — Playwright tests for full workflow: add project → view sprints → kanban → sprint detail → findings → roadmap. Test with real sprint-forge directory structure.
  5. Documentation & Memory Update — Update MEMORY.md with new architecture. Update CLAUDE.md instructions if needed. Clean up old mock data references.

---

## Phase 2 — From Viewer to Cockpit (Growth-CEO Initiatives)

> **Strategic source**: `.agents/growth-ceo/strategic-overview.md` and initiatives `0001`, `0002`, `0003`.
>
> **Focal question**: "What is the minimum write surface that transforms Kyro from a passive dashboard into an active command center for AI-driven development?"

### Sprint 7 — Universal Search & Command Palette Enhancement

- **Source**: `findings/09-live-search-and-cross-project-intelligence.md`
- **Version Target**: 2.0.0
- **Type**: feature
- **Focus**: Transform Cmd+K from navigation-only to a universal search across all parsed data
- **Dependencies**: Sprint 6 (completed)
- **Strategic Alignment**: Growth-CEO Initiative 2 (Live Search + Cross-Project Intelligence) — "This alone makes Kyro 5x more useful"
- **Suggested Phases**:
  1. Search Index — Build a search index function that flattens store data (tasks, findings, debt items, sprints, documents, phases) into typed searchable entries with navigation targets. Each entry: `{ type, title, description, metadata, projectId, navigateTo }`.
  2. Fuzzy Matching Engine — Implement scoring/filtering logic that works with `cmdk`. Support prefix matching, substring matching, and relevance scoring. Group results by type (Tasks, Findings, Debt, Sprints, Documents).
  3. Command Palette Overhaul — Rebuild `command-palette.tsx` to show search results grouped by type alongside existing navigation commands. Each result navigates to exact view (sprint detail → section, finding detail, debt dashboard). Add keyboard shortcuts for result type filtering.
  4. Topbar Search Integration — Replace placeholder search input in `app-topbar.tsx` with Cmd+K trigger. Show recent searches or quick filters. Wire the search input to open the command palette with pre-filled query.
  5. Findings Page Search — Add severity filter and text search to findings-page.tsx (addresses Sprint 6 Rec 3). Reuse the search index logic.

### Sprint 8 — Kanban Persistence & Structured Write Actions

- **Source**: `findings/10-command-bar-structured-actions-and-kanban-persistence.md`
- **Version Target**: 2.1.0
- **Type**: feature
- **Focus**: Break the read-only barrier — persist kanban state and add write actions to Cmd+K
- **Dependencies**: Sprint 6 (completed)
- **Strategic Alignment**: Growth-CEO Initiative 1 Phase 1 — "Wire kanban persistence... massive UX improvement"
- **Suggested Phases**:
  1. Task Mutation Service — Add `updateTaskStatus(projectId, sprintId, taskId, status)` to service interface + file implementation. Wire to existing `PUT /api/projects/[id]/sprints/[sprintId]/tasks/[taskId]` route. Ensure the API route actually writes status changes back to the sprint markdown file.
  2. Kanban Persistence — Wire dnd-kit `onDragEnd` in `sprint-board-page.tsx` to call the mutation service. Implement optimistic update with rollback (follow existing `updateProject` pattern in store). Add activity logging for task status changes.
  3. Confirmation Dialog Component — Create a reusable `ActionConfirmDialog` that shows what will change (action type, target, before/after). Used by all write operations for safety.
  4. Command Palette Actions — Add "Actions" group to Cmd+K: "Update task status" (shows task picker + status picker), "Create document" (title + content dialog). Each action goes through confirmation dialog before executing. Wire to existing unused API routes.
  5. Git Safety Net — Before any write operation, auto-commit current state via `POST /api/workspace/git/commit` (endpoint exists). This ensures every mutation has a rollback point. Show "last saved" timestamp in UI.

### Sprint 9 — Sprint Forge Trigger & Prompt Composer

- **Source**: `findings/11-sprint-forge-trigger-from-kyro.md`
- **Version Target**: 2.2.0
- **Type**: feature
- **Focus**: Close the workflow loop — generate sprint-forge prompts from within Kyro
- **Dependencies**: Sprint 6 (completed)
- **Strategic Alignment**: Growth-CEO Initiative 3 — "Closes the loop entirely... no terminal needed"
- **Suggested Phases**:
  1. Sprint Context Assembler — Build a `lib/forge/context.ts` module that reads current project state and assembles sprint-forge context: last sprint summary, open debt items, unresolved findings, next sprint number, version target. Pure function, no side effects.
  2. Prompt Composer — Build `lib/forge/prompt-composer.ts` that takes assembled context + user selections (which findings, which debt items, version target) and outputs a ready-to-use sprint-forge prompt string. Follow the sprint-forge re-entry prompt format.
  3. Sprint Generation Wizard UI — Add "Generate Next Sprint" button on roadmap-page.tsx. Opens a multi-step dialog: Step 1 select findings (checkboxes), Step 2 select debt items to address, Step 3 set version target + sprint type, Step 4 preview composed prompt. "Copy to Clipboard" button.
  4. Project Refresh — Add a "Refresh Project" action (button in topbar + Cmd+K command) that re-reads the project directory from disk. Detects new sprint files, updated findings, changed ROADMAP.md. Updates store without full page reload.
  5. Sprint Forge Integration Page — Replace or enhance the current "Agents Activity" page with a "Sprint Forge" page. Show: generation history, last prompt used, project health metrics (open debt count, finding resolution rate, sprint velocity). This becomes the command center.

### Sprint 10 — AI Instruction Layer

- **Source**: Convergence of findings `09`, `10`, `11` + Growth-CEO Initiatives 1 Phase 2 + Initiative 3 Week 2
- **Version Target**: 3.0.0
- **Type**: feature
- **Focus**: Add AI-powered natural language actions and autonomous sprint generation
- **Dependencies**: Sprint 7, Sprint 8, Sprint 9
- **Strategic Alignment**: Growth-CEO Initiative 1 Phase 2-3 + Initiative 3 Month 2
- **Suggested Phases**:
  1. AI Interpret Route — Add `/api/ai/interpret` route that takes natural language input + project context, calls Claude API, returns structured action intent `{ action, params, preview }`. Support: "generate sprint", "update task", "create finding", "mark debt resolved".
  2. Smart Command Mode — Extend Cmd+K with "smart mode": if input doesn't match a known command or search result, route to `/api/ai/interpret`. Show AI-generated preview of the proposed action. User confirms → execute via existing action routes from Sprint 8.
  3. Forge Generate Route — Add `/api/forge/generate` that writes the composed prompt to the project directory. Optionally detect and invoke `claude` CLI if installed. Return generation status (started, in-progress, complete).
  4. Generation Monitor — Real-time UI that polls the project directory for new sprint files during generation. Show progress indicators. Auto-refresh and display the new sprint when detected. Log generation as activity.
  5. Action Chaining — Allow AI to chain multiple actions: "analyze findings and generate next sprint" → assembles context → composes prompt → triggers generation → monitors → displays result. Each step logged in activity trail.
